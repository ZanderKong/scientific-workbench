# 科研工作台执行入口

## 必读顺序
1. HANDOFF.md（当前工作与接续点）。
2. IMPLEMENTATION_PLAN.md（已批准范围与任务门槛）。
3. DESIGN_RULES.md、docs/PAGE_ACCEPTANCE.md、docs/DATA_PROTOCOL.md。
4. docs/VERIFICATION.md 及当前任务关联的测试。

产品行为遵循已批准计划及有效设计规则；视觉遵循 prototype/reference.html（哈希见 prototype/manifest.json）。现有应用和历史实现声明不是设计依据。不要重新设计界面、缩减范围或自动创建猜测类别的对象。

## 实施规范
- 原始 index.html 与冻结原型禁止修改。迁移 DOM/CSS 后再接真实业务。样式按组件限定，禁止裸 aside/header 的卡片装饰。
- 模块采用正常格式与明确类型；禁止巨型压缩 JSX、吞掉保存异常、放宽类型或削弱测试以通过门槛。
- 测试使用专属临时工作区和独立端口；不启动或修改 ~/ScientificWorkbench。不得把实际用户工作区当成 demo。
- 不在 SQLite 独占保存业务事实。文件/清单完整，索引可重建；旧格式转换到新目录，原目录保留。
- 不把阶段完成称为首版交付。未执行的 IME/S3/视觉验收保持待验收。
- 阶段完成、上下文切换前更新 HANDOFF.md：任务编号、证据、失败、临时措施、服务状态和下一步。
- 不自动切换模型；任何模型从同一个未完成任务接续。未获明确授权不派生子代理。

## 必须验证
正文实际落盘和重开、版本冲突、稳定区块、对象显式创建、Data 多入口、证据冻结、无索引重建、无旧目录恢复、凭据排除以及原型逐状态对照。
