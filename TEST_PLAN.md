# 科研工作台验收测试计划

> 版本：v0.4  
> 对应规则：[DESIGN_RULES.md](./DESIGN_RULES.md)  
> 状态：所有项目在实现前均为未验证；原型中的 toast 或内存状态不计为通过。

## A. 编辑器核心

- [ ] `A1-01` 新建 Sample 直接出现一级 bullet；不要求输入 `-`。
- [ ] `A1-02` Enter 创建同级，Tab/Shift+Tab 调整层级；普通科研结构不能无限缩进，Data 允许受限第三层。（`EDIT-002`）
- [ ] `A1-03` 一级父 bullet 可声明原料、样品、过程、设备；子 bullet 用 `[对象]｜属性：值` 把实际值写入当前 Sample。（`EDIT-001`、`EDIT-002`）
- [ ] `A1-04` 粘贴多行文本、中文 IME、语音转写和完全没有引用语法的普通文本都能完整保存。（`EDIT-004`）
- [ ] `A1-05` 重复引用且同一属性文本不同触发轻量提醒；相同文本、不同属性、父级声明加单次属性不误报，不阻止保存或自动拆分。（`EDIT-003`）
- [ ] `A2-01` Data Detail Claim Editor 的一级 bullet 创建正式 Claim，host 为当前 Data。（`CLAIM-002`、`CLAIM-003`）
- [ ] `A2-02` Analysis/View Claim Editor 的一级 bullet 创建正式 Claim，host 为当前 View。（`CLAIM-002`、`CLAIM-003`）
- [ ] `A2-03` Claim Editor 与 Bullet Engine 共用层级、Enter、Tab 和属性编辑行为。（`CLAIM-003`）
- [ ] `A2-04` Sample 编辑器任何层级的 `[论点]` 都作为文本保存，不进入 Claim 数据库。（`CLAIM-001`）
- [ ] `A2-05` Sample 中 `[论点]` 就地显示：“只有在数据或分析页面记录的论点，才会正式保存为可引用的论点。此处记录缺少直接的证据关联，仅作为文本保留。”不阻断输入。（`CLAIM-001`）

- [ ] `A1-06` 子级引用父级未声明对象时有效属性正常投影，操作引用含该对象，轻量提醒补父级而不改写。（`EDIT-005`）

## B. Object Picker 与引用

- [ ] `B1-01` Sample、原料、设备、Process 进入同一搜索池，结果显示 role 胶囊；Sample 可搜索和引用。（`OBJ-002`）
- [ ] `B1-02` 完全匹配优先于前缀、Alias、包含和模糊匹配；最近/常用只作为次级排序。（`OBJ-005`）
- [ ] `B1-03` 已废弃对象始终可见但显著降权；无正常结果时仍能选择。（`OBJ-004`）
- [ ] `B1-04` 无匹配时提供创建原料、设备、过程，不提供创建样品；选择只产生 create intent，不因临时输入立刻创建永久对象。（`OBJ-003`）
- [ ] `B1-05` 输入左方括号开启补全、选中自动闭合为 `[标准名]`，光标在右括号后；修改名称解除旧绑定、重新匹配，括号破坏后为普通文字。（`OBJ-001`）

- [ ] `B1-06` 接受 `[名称]`、`【名称】`、`［名称］`；允许空格/连字符/圆括号；错配、未闭合、嵌套方括号不形成合法引用，原文保留；普通圆括号不触发引用。（`OBJ-001`）
- [ ] `B1-07` 粘贴完整引用按唯一标准名绑定；未匹配或重名标记待绑定，不自动创建永久 Object。（`OBJ-001`、`OBJ-003`）
- [ ] `B1-08` 同一左括号菜单提供对象及数据/论点行；`[数据]`、`[论点]` 及对应括号变体只在行首作为类型标记，Sample 论点仍只保留文本；附件文件名不重新解析为对象。（`EDIT-007`、`DATA-001`、`CLAIM-001`）

## C. Document-first 持久化

- [ ] `C1-01` 输入过程 autosave 原始 Document，autosave 不依赖 Parser 成功；Parser 出错时正文仍可恢复。（`CORE-001`、`PARSE-001`）
- [ ] `C1-02` 浏览器刷新、意外退出、页面关闭或导航后，已保存正文可恢复；输入无明显卡顿。
- [ ] `C2-01` 及时保存正文；完成编辑/切页后提取，离开一个父 bullet 不要求解析；编辑期间仅提示结果待更新。（`PARSE-001`）
- [ ] `C2-02` 旧解析结果不得覆盖更新后的正文索引；待更新状态持久化，关闭/崩溃后下次启动可重试。（`PARSE-001`、`PARSE-002`）
- [ ] `C2-03` autosave 300–800 ms 为待验证建议，不把逐操作或 idle 解析当作必需验收。（`PARSE-004`）
- [ ] `C3-01` 首版整篇提取，操作仅作为来源定位；有效部分正常提交，无效部分保留文字。（`PARSE-002`）
- [ ] `C3-02` 重复解析更新原记录，不重复创建对象、属性值或 Data；失败可从已保存正文重试。（`PARSE-002`、`PARSE-003`）
- [ ] `C3-03` 提取更新隐藏文件头和索引，不自行改写正文；新建对象/属性/Data 可先保存预备意图，完成编辑后再提交。（`PROJ-001`、`PARSE-003`）

## D. Structured Projection 与属性

- [ ] `D1-01` 样品文件包含稳定元数据、提取结果和正文；详情直接打开只渲染正文的编辑器，索引记录属性来源用于跳转。（`PROJ-001`）
- [ ] `D1-02` 投影失败不破坏正文，旧 projection 不覆盖新 revision。
- [ ] `D2-01` 多条文本值以 `80 g · 20 g` 展示，独立保存各自值与来源，显示分隔符不写进单条值。（`SAMPLE-002`）
- [ ] `D2-02` `30 min`、`过夜`、`约半小时`、`80–100 ℃` 全部保存文本；首版无 sum/聚合、单位换算、数值范围筛选；`[搅拌器]｜添加量：5 g` 不因科学适用性报错。（`CORE-002`、`SAMPLE-002`）

## E. Object 生命周期

- [ ] `E1-01` Rename 保持 object_id，引用自动显示新 canonical name，不重写过去的正文快照，保留 raw_text provenance。（`LIFE-001`）
- [ ] `E2-01` Merge 重定向全部 ReferenceOccurrence，旧对象保存 redirect_to 并标记 merged/deprecated；Projection 自动重算，正文不改写。（`LIFE-002`）
- [ ] `E3-01` 第一次删除变为 Deprecated；历史引用可打开并显示“已废弃”；搜索中仍可找到但低优先级。（`LIFE-003`）
- [ ] `E4-01` 有 inbound reference 时阻止永久删除，展示引用位置；处理引用后删除不得留下 dangling reference。（`LIFE-003`）

## F. Sample 创建、复制与数据库

- [ ] `F1-01` 缺省编号使用 `YYMMDD-01` 起的当日最大值加一，不复用废弃/删除编号；手改编号唯一性校验。（`SAMPLE-001`）
- [ ] `F2-01` 复制 Sample 保留 RecordUnit、对象/Process 引用、条件属性和步骤结构；排除 Data、DataComponent/文件、Claim 及 Claim 属性。（`SAMPLE-001`）
- [ ] `F3-01` 批量新建使用去结果模板，一行一个独立 Sample/Document；修改生成行不影响模板或其他行。（`SAMPLE-001`）
- [ ] `F4-01` 样品属性表列来自 Object+Property；长名称 ellipsis，hover 显示完整名称，列拖拽和单元格居中正常。（`SAMPLE-002`）

## G. Data

- [ ] `G1-01` Data 与 File 分离；一个 Data 可有多个 DataComponent，一个文件一个子 bullet。（`DATA-001`）
- [ ] `G1-02` Data 直接导入默认名为 `YYYY-MM-DD-数据`；Data Detail 可查看来源、组件和 About Sample。（`DATA-001`）
- [ ] `G1-03` Sample 编辑器的 Data 子级可记录文件附件和普通文本；其中 `[论点]` 仍只保存文本。（`DATA-002`、`CLAIM-001`）
- [ ] `G2-01` 外部 AI 经 API/MCP 新增派生成果，不覆盖 raw；内置 AI 与强制 proposed/accepted/rejected 工作流不是首版已定功能。（`DATA-003`、`AI-001`）
- [ ] `G2-02` 派生成果保留来源；事实趋势与正式 Claim 入口分开，具体元数据随接口计划验收。（`DATA-003`）

- [ ] `G1-04` Data 双向同步，每次提交检查版本，旧页面不能静默覆盖；版本检查不要求完整历史回滚。（`DATA-004`）
- [ ] `G1-05` 删除样品 Data 区块仅移除当前引用，Data、上传文件和原始来源追溯保留，仍可从数据页找到。（`DATA-004`）

## H. Analysis / View

- [ ] `H-01` Sample 多选新建 Analysis，所选 Sample 自动成为 View.items；Data 多选同理。（`VIEW-001`）
- [ ] `H-02` View 可组织多个 Sample/Data、比较表、图和 Artifact，不拥有被引用对象。（`VIEW-001`）
- [ ] `H-03` Analysis 中正式 Claim 的 host 为当前 View；从 View 打开 Sample/Data 后返回原 View。（`VIEW-001`、`CLAIM-003`）
- [ ] `H-04` 分析显示最新内容；正式论点固定创建时证据，之后 Data 修改不改变已保存证据内容和所引用附件。（`VIEW-001`、`CLAIM-003`）

## I. 资源、属性和范围边界

- [ ] `I-01` 原料、设备和 Process 的 role/tag 语义一致；Sample 可引用但不能通过引用菜单内联创建。（`RES-001`）
- [ ] `I-02` 全局共用属性字典；仅名称即可创建，量纲/单位/别名可选。软件不从值推断量纲、不校验单位，不自动分析近义词或合并。（`RES-002`）
- [ ] `I-03` 产品不要求用户把所有记录写进每日文档；外部记录、语音或 AI 整理后的规范文档可进入 Sample Document。（`SCOPE-001`）

## J. Parser / Resolver 边界

- [ ] `J-01` 普通文本“水”“搅拌”不自动绑定 Object；Parser 只解析显式 `[对象]`。（`EDIT-004`、`OBJ-001`）
- [ ] `J-02` ambiguous、unresolved、冲突和显式创建候选项可定位到对应操作；不静默绑定或创建错误对象。（`PARSE-003`）
- [ ] `J-03` AI 可把自由文本规范化为 `[对象]`、`｜属性:值`、`[数据]` 和 Data 子级，并与人工文档共用同一确定性 Parser。（`EDIT-004`）
- [ ] `J-04` 解析版本升级可重新提取当前文档；具体迁移验收由编码计划补充，不要求通用历史差异审计。（`PARSE-002`）

## K. 性能与稳定性

- [ ] `K-01` small/medium/large 文档基准分别覆盖 20/100/500 RecordUnit；记录输入延迟、autosave、parse 和 projection 时长。
- [ ] `K-02` 100 个 RecordUnit 编辑无可感知卡顿，500 个 RecordUnit autosave 不阻塞输入；Parser 可异步完成并显示 pending。（`PARSE-001`、`PARSE-002`）
- [ ] `K-03` 5000/50000 Object 下 方括号补全查询仍可用；多文件 Data、长期 revision 增长和列表重投影有可测基线。
- [ ] `K-04` `1 g` 与 `1000 mg` 作为不同文本测试结构提醒，不做单位等价或科学判断。（`EDIT-003`）

## L. 原型回归与差异记录

- [ ] `L-01` 原型页面导航、样品/数据/分析/Claim/资源详情和 `•••` 菜单可打开；只作为交互回归，不视为持久化通过。
- [ ] `L-02` 长对象名/属性名 ellipsis、hover tooltip、列横向滚动、排序和拖拽视觉正常。
- [ ] `L-03` 原型中的复制、批量新建、多选建分析、导入和导出操作在底层实现后补充真实数据验收；当前 toast 不计通过。

## M. 首版运行范围

- [ ] `M-01` 个人本机启动、浏览器访问、明确数据目录；附件先本地保存，可选 S3，网盘/GitHub/多人协作后置。（`SCOPE-002`、`STORE-001`）

## N. 属性字典、语法和输入

- [ ] `N-01` 支持 `｜名：值`、`|名:值` 及混合全半角；空名、空值、缺冒号不形成属性或字典条目，正文保留；无效段不影响其他有效段。值可含冒号，竖线只作分隔、不支持转义。（`PROP-001`）
- [ ] `N-02` 有效属性在完成编辑后的文档解析中无弹窗匹配/创建；离开单个操作不要求创建，半成品不创建。（`PROP-001`、`PARSE-003`）
- [ ] `N-03` 用户或 AI 明确填写的别名参与搜索；选中“添加量”插入标准名。不选菜单直接写“用量”时，按标准名精确匹配或创建“用量”，不通过别名静默合并。（`RES-002`）
- [ ] `N-04` 竖线后按最近使用、再高频补充去重，输入后模糊搜索；中文组词期间不刷新、不抢方向键/Enter，确认后查询。覆盖数字/英文/符号后切中文、Esc 关闭继续输入，以及对象补全。（`EDIT-006`）
- [ ] `N-05` 属性空值位置显示配置的灰色推荐单位，未配置不显示；提示不保存，输入后隐藏，不自动补入单位、不校验值。（`PROP-002`）

## O. 当前值、引用与轻量历史

- [ ] `O-01` 将 30 min 改为 60 min 后只出现更新后的当前值；删除属性行移除提取值但保留字典；无独立属性历史库。（`EDIT-008`）
- [ ] `O-02` 删除父级移除子级和对应引用/提取结果，独立 Data/文件保留；修改父级保留未修改对象属性和 Data。（`EDIT-008`、`DATA-004`）
- [ ] `O-03` 三个搅拌操作可分别跳转，父级与子级同一搅拌不重复计作两次操作；无需通用关系管理界面。（`EDIT-008`）
- [ ] `O-04` 过去正文可查看、复制，没有一键恢复/跨实体回滚；文件头更新不应制造无意义正文历史。生成与保留默认策略按编码计划验收。（`HISTORY-001`）
- [ ] `O-05` 论点证据快照与普通正文历史分开，快照附件内容可读取，即使迁往 S3 也不改变引用。（`CLAIM-003`、`STORE-003`）

## P. 外部 AI、分析导出与新鲜度

- [ ] `P-01` 应用不内置 AI；API/MCP 通过共同业务操作管理字典、文档、Data/附件、Analysis/Claim、检索导出及存储备份，具体权限与能力清单在编码计划验收。（`AI-001`）
- [ ] `P-02` 分析导出包含涉及样品/Data 的实际正文、关联与来源、附件清单，不只有名称或链接；外部 AI 能继续读取文件。（`VIEW-001`、`AI-001`）
- [ ] `P-03` 打开关联 Data、导出分析、API/MCP 读取最新结构前处理相关待更新文档；提取失败不得静默返回旧结构为最新。（`PARSE-001`）

## Q. 本地与 S3 存储、备份恢复

- [ ] `Q-01` 附件上传时立即可靠落本地，早于完成编辑解析；S3 未启用时本地使用正常。（`STORE-001`）
- [ ] `Q-02` S3 自动上传同时满足大小阈值与首次本地保存满 X 天，默认 X=15；大小默认值目前仅建议 100 MB，最终由编码计划明确。（`STORE-001`、`STORE-002`）
- [ ] `Q-03` 不足 15 天的大文件、满 15 天但未超过大小阈值的文件不上传；查看/引用不重置年龄，无“未访问”策略。（`STORE-002`）
- [ ] `Q-04` 应用关闭无任务执行，下次启动检查到期任务；没有常驻后台服务。（`STORE-002`）
- [ ] `Q-05` 上传失败/校验失败保留本地；成功后按用户设置保留双份或远端索引，按需读取可用；临时访问 URL 过期不破坏稳定引用。（`STORE-003`）
- [ ] `Q-06` 删除文档引用不删除 S3 对象；专门清理检查 Data/论点证据仍有无使用。（`STORE-003`）
- [ ] `Q-07` 手动全量备份到本地或 S3，绕过附件阈值/等待天数；包含所有正文、字典、Data、分析、历史、证据和实际附件，可独立恢复。（`STORE-004`）
- [ ] `Q-08` 仅远端附件也被下载纳入备份；缺失/下载失败时标明不完整，不能报完整成功；恢复一致性和凭据行为按编码计划验收。（`STORE-004`）

## R. 完整实验样例

- [ ] `R-01` 建立覆盖输入、创建、修改删除、重复解析、Data 冲突、分析导出、API/MCP、证据与备份恢复的完整样例；参考论文注明来源并区分真实事实和模拟数据。（`VERIFY-001`）
