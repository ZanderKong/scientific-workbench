# 讨论与决策记录

本文件记录讨论过程中的产品决策。2026-09-11 补记至轻量历史、延后提取和 S3 延迟上传定案；历史条目的旧语法仅用于说明决策沿革。当前行为以 [DESIGN_RULES.md](./DESIGN_RULES.md) 为准；这里不直接替代规则正文。

## 记录格式

每条记录包含日期、状态、结论、原因和影响。状态使用“已确认 / 建议 / 待讨论 / 已替代”。被替代的方案仍保留，避免后来重新讨论时丢失上下文。

## 已确认决策

### DEC-001 · 父 bullet 的操作粒度

- 日期：2026-09-11
- 状态：已确认
- 结论：建议用户让一个父 bullet 表达一个完整操作，例如一次搅拌、一次加热或一次添加；不要把过于复杂的复合操作塞进一个父 bullet。
- 影响：父 bullet 是自然解析单元；它可以在同一级声明本次操作涉及的原料、样品、过程和设备。
- 关联规则：`EDIT-001`、`PARSE-001`
- 关联验收：`A1-03`、`C3-01`

### DEC-002 · 样品属性的归属

- 日期：2026-09-11
- 状态：已确认
- 结论：对象通过父级 `@` 引用进入操作；子 bullet 用 `@对象｜属性名：属性值｜属性名：属性值` 指明属性。写入的是当前 Sample 的实际属性和值，不是把这次值改成原料、设备或过程对象的全局属性。
- 影响：属性投影必须保存 Sample、对象、操作、原文位置和 revision 的关系。
- 关联规则：`CORE-002`、`EDIT-002`、`PROJ-001`
- 关联验收：`A1-03`、`D1-01`

### DEC-003 · 重复对象与复合操作提醒

- 日期：2026-09-11
- 状态：已确认
- 结论：同一父 bullet 中出现同名对象，且同一属性有不同值时，解析完成后提示操作可能过于复合。只提醒，不阻止保存，不自动拆分。
- 影响：提示是结构建议，不是 Parser 的自动改写结果。最新确认首版只比较文本，不作单位等价判断（见 DEC-010）；同名对象但属性不同不触发该提示。
- 关联规则：`EDIT-003`、`PARSE-003`
- 关联验收：`A1-05`、`K-04`

### DEC-004 · Sample 编辑器中的 Claim

- 日期：2026-09-11
- 状态：已确认
- 结论：Sample 编辑器中任何层级的 `@claim` 都作为普通文本保存，不创建正式 Claim。
- 提示文案：只有在数据和分析页面记录的论点会被正式记录并在后续被引用，此处的记录缺少直接的证据关联，所以仅作文本记录。
- 影响：即使 `@claim` 出现在 `@data` 的子 bullet 中，也不进入 Claim 数据库；Data 子级仍可承载文件附件和普通文本。
- 关联规则：`CLAIM-001`、`CLAIM-002`
- 关联验收：`A2-04`、`A2-05`、`G1-03`

### DEC-005 · 正式 Claim 的入口

- 日期：2026-09-11
- 状态：已确认
- 结论：只有 Data Detail 和 Analysis/View Detail 的 Claim Editor 可以正式创建 Claim。Data 创建时 host 为当前 Data；Analysis 创建时 host 为当前 View。
- 影响：Claim 数据库只浏览和检索，不提供全局新建入口。host 是来源/上下文/证据入口，但不代表论点已经被证明；需要历史追溯时可固定 revision。
- 关联规则：`CLAIM-002`、`CLAIM-003`
- 关联验收：`A2-01`、`A2-02`、`H-03`

## 已替代方案

### DEC-006 · Sample 的 Data 子级直接创建正式 Claim

- 日期：2026-09-11
- 状态：已替代
- 原方案：Sample 中 `@data` 的直接子 bullet 可以通过 `@claim` 创建正式 Claim，host 为该 Data。
- 替代原因：样品操作记录缺少直接的证据关联；这类记录应保留为文本，正式 Claim 要在 Data 或 Analysis 页面创建。
- 当前方案：见 `DEC-004`、`DEC-005`。

### DEC-007 · Object mention 在补全后移除 `@`

- 日期：原型历史行为
- 状态：已替代
- 原方案：Object Picker 选中后把 `@对象` 替换成不带 `@` 的轻量 token。
- 替代原因：`@对象` 是持久化的正式文档语法，导出、AI 处理和确定性 Parser 都需要它。
- 当时替代方案：持久化保留 `@`。该方案又被 DEC-013 的统一单括号语法替代。

## 后续已确认决策（2026-09-11）

### DEC-008 · 个人本机首版

- 状态：已确认
- 决策：个人使用、本机运行、浏览器访问，数据库和附件保存本机。
- 原因：先完成个人科研工作流，团队协作后置。
- 关联规则：`SCOPE-002`；验收：`M-01`。

### DEC-009 · Data 多入口与删除区块

- 状态：已确认
- 决策：同一 Data 在样品与 Data 页面同步编辑；删除样品区块只移除引用，保留 Data、文件和原始来源追溯。
- 原因：数据应能独立管理和继续引用，文档区块的删除不等于销毁数据。
- 关联规则：`CORE-001`、`DATA-004`；验收：`G1-04`、`G1-05`。

### DEC-010 · 文本属性与结构提醒

- 状态：已确认
- 决策：属性全部保存为文本，多值以 · 分隔展示；首版取消 sum、数值聚合、单位换算和数值范围筛选。软件不核查科学事实或对象与属性是否适配。
- 原因：降低首版开发复杂度，允许“过夜”“约半小时”等科研表达。同名对象多次出现且同一属性文本不同仍可结构提醒，因其无需科学语义理解。
- 关联规则：`CORE-002`、`EDIT-003`、`SAMPLE-002`；验收：`A1-05`、`D2-01`、`D2-02`、`K-04`。

### DEC-011 · 全局属性字典与手动别名

- 状态：已确认
- 决策：全部对象共用字典；新属性无需弹窗，在有效属性解析提交时直接创建。量纲和单位仅作可选提示。别名由用户或 AI 明确填写，选中已有结果插入标准名。
- 匹配：未使用菜单时按实际标准名精确匹配，否则创建新名字。别名只帮助补全，软件不自动分析近义词、不自动绑定别名或合并；属性合并功能后置。
- 关联规则：`RES-002`、`PROP-001`、`PROP-002`；验收：`I-02`、`N-01`、`N-02`、`N-03`。

### DEC-012 · 严格属性格式与 IME

- 状态：已确认，作为首版方案
- 决策：竖线后展示最近/高频属性，输入后模糊搜索；支持全角半角竖线和冒号，但名称和值必须非空。无效段只作文字，有效段独立解析；值中不含竖线，不做转义。
- 输入保护：中文组词期间不刷新菜单、不抢方向键或 Enter，确认后再查询；覆盖数字/英文/符号后切中文。灰色单位提示不进入正文，输入值后隐藏。
- 关联规则：`EDIT-006`、`PROP-001`、`PROP-002`；验收：`N-01` 至 `N-05`。

### DEC-013 · 对象、Data、Claim 统一单括号

- 状态：已确认
- 决策：对象 `[名称]`；行首类型标记 `[数据]`、`[论点]`。兼容 `【名称】`、`［名称］`；菜单统一插入半角方括号并自动闭合。
- 原因：名称可以包含空格、数字、连字符和圆括号；边界明确，输入无需强制替换名称。普通备注用圆括号，首版不支持嵌套方括号或错配括号。
- 绑定：完整引用唯一标准名匹配时绑定；未匹配或重名则待绑定、不自动创建。修改名称解除旧绑定，破坏括号后作为普通文字。
- 行为边界：Sample 中论点仍只保存文本。Data 附件由专用组件呈现，不把文件名重新当对象解析。
- 关联规则：`OBJ-001`、`EDIT-007`、`CLAIM-001`；验收：`B1-05` 至 `B1-08`、`A2-04`。

### DEC-014 · 父级未声明的子级对象

- 状态：已确认
- 决策：子级正常结构化，操作引用包含该对象，轻量提醒补充父级，不自动改写父级。
- 关联规则：`EDIT-005`；验收：`A1-06`。

## 补充已替代方案

- **旧 @ 语法（已替代）**：`@对象`、`@data`、`@claim`，以及讨论中的 `@[名称]`、双括号和强制空格改连字符方案，不是当前标准；当前见 DEC-013。
- **数值属性聚合与换算（已替代）**：首版 `multi | sum`、自动量纲推断、单位等价比较被 DEC-010/011 替代。
- **属性唯一别名自动绑定（已替代）**：菜单仅补全，正文匹配按 DEC-011/012。
- **固定短 idle / 离开操作子树作为主要解析触发（已替代）**：当前为完成编辑后整篇提取，见 DEC-015。
- **原文不可因任何动作更新（澄清）**：Parser 不自行改写正文，但用户编辑同一 Data 的多入口同步属于已授权的内容更新，见 DEC-009。

## 最新已确认决策（2026-09-11）

### DEC-015 · 文档为主、延后整篇提取

- 状态：已确认，首版试行
- 原因：保持轻量，避免把可提取的属性设计为复杂关系系统。
- 结论：正文及时保存，完成编辑/切页后整篇提取；创建可在编辑时先记录预备动作，解析时提交。有效部分生效，无效文字保留；重复解析更新原记录。
- 崩溃处理：持久化待更新状态，下次启动重试；打开 Data、导出分析及 API/MCP 读取最新结构先处理相关待更新正文。
- 关联规则：`PARSE-001` 至 `PARSE-004`；验收：`C2-01` 至 `C3-03`、`P-03`。

### DEC-016 · 文件头与正文

- 状态：已确认
- 结论：表头是文件内部元数据，编辑器只展示正文；数据库用于检索和来源跳转，提取结果可重建。
- 边界：稳定身份与生成结果分开，YAML/Markdown 只是候选格式；准确存储协议在编码计划选择。
- 关联规则：`PROJ-001`；验收：`D1-01`。

### DEC-017 · 修改、删除和重复操作

- 状态：已确认
- 结论：值修改即更新，删除属性保留字典。删除父级移除子级及其文档引用；修改父级保留未改对象属性和 Data。独立 Data/文件只解除引用。
- 次数：三个操作用了搅拌表示三次使用；同一步父级声明加子级属性不是两次。
- 关联规则：`EDIT-008`、`DATA-004`；验收：`O-01` 至 `O-03`。

### DEC-018 · 最轻量历史与 Data 防覆盖

- 状态：已确认
- 结论：正文快照仅查看/复制，不做一键恢复或通用关系回滚；独立属性历史取消。Data 提交检查版本，旧页面不能静默覆盖。
- 默认建议：完成编辑且正文变化时存快照，文件头单独变化不新增；保留策略由计划确定。
- 关联规则：`HISTORY-001`、`DATA-004`；验收：`O-04`、`G1-04`。

### DEC-019 · 分析导出、证据与外部 AI

- 状态：已确认
- 结论：Analysis 首版组织、比较、记录并导出实际文档内容，浏览最新数据；正式 Claim 保存创建时所用证据快照。
- AI：应用不内置 AI，各功能提供 API 和 MCP，由外部 AI 操作；先前内置 proposal UI 并未定案。
- 关联规则：`VIEW-001`、`CLAIM-003`、`AI-001`、`DATA-003`；验收：`H-04`、`P-01`、`P-02`、`O-05`。

### DEC-020 · 本地与 S3 大附件延迟上传

- 状态：已确认
- 结论：首版本地＋S3 兼容存储。文件上传先落本地；启用后仅“超过大小阈值且保存满 X 天”的附件自动上传，X 默认 15 天，可配置；100 MB 只是建议大小默认值。
- 生命周期：年龄从首次本地保存成功计算，不看访问/引用频率；关闭不运行，下次启动处理，无常驻服务。成功核对后可选双份或本地仅留远端索引，失败保留本地。删除文档引用不联动远端删除。
- 关联规则：`STORE-001` 至 `STORE-003`；验收：`Q-01` 至 `Q-06`。

### DEC-021 · 可选手动完整备份

- 状态：已确认
- 结论：明确本机数据目录；手动全量备份输出本地或上传 S3，不受日常阈值/年龄限制，包含全部附件实际内容，可独立恢复。远端缺件不能报告完整成功。
- 替代：默认全量自动上传、类型/大小/未访问多条件策略、首版网盘与 GitHub 接入均后置。
- 关联规则：`STORE-004`；验收：`Q-07`、`Q-08`。

### DEC-022 · 编码准备与完整样例

- 状态：已确认的交付要求
- 结论：实现方设计完整科研验收样例，可参考公开论文，但标明模拟数据。目标是尽量完整的初版，可试用调整细节；不扩大为通用关系平台。
- 关联规则：`VERIFY-001`、`SCOPE-002`；验收：`R-01`。
- 当前状态：产品约束足以开始 coding 计划；技术栈、格式、接口清单及样例尚未产出，不等于可以跳过实施设计直接宣称完成。

## 待在编码计划收敛

- 编辑器和文件协议、区块/引用稳定标识、文件头与索引一致性。
- Data 版本冲突、解析失败重试、创建撤销与重名提示的最小实现。
- API/MCP 连接授权和工具覆盖、分析导出范围、证据快照格式。
- 轻量历史生成/清理默认值；多 Sample About、复制模板中的普通论点文字如何处理。
- S3 凭据保存、大小默认值与单位、调度/校验/重试、远端缓存、本地目录和恢复流程。
- 具体技术栈、启动交付方式、完整样例和分阶段验收。

以上优先给出实施建议，仅对影响产品语义的分歧追加少量问题，不再重复已确认的基础规则。
